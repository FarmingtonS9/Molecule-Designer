pub trait WaveFunction {
    fn schrodingers_equation() {}
}

pub struct Electron {
    spin: bool, //Spin state of electron. Either "up" (0, false) or "down" (1, true)
}

pub struct Proton;

pub struct Neutron;
